# agent-sdlc-workflow v2.0 最终方案裁定

- **状态**：最终设计裁定，作为 v2.0 后续实施的唯一基线
- **日期**：2026-07-23
- **代码基线**：`45e36b9`（`master`）
- **输入文档**：
  - `修复方案-v2.0.md`（Claude 初始方案）
  - `项目审计与详细修复方案.md`（Codex 审计方案）
  - `对Codex审计方案的反思与评分.md`（Claude 反思与妥协方案）
- **裁定原则**：不按作者站队，只按事实、风险、实施成本和可验证效果取舍

---

## 0. 文档效力

当三份输入文档与本文冲突时，以本文为准。

三份输入文档保留为：

| 文档 | 后续地位 |
|---|---|
| Claude 初始方案 | 问题诊断与历史设计来源 |
| Codex 审计方案 | 概念模型、安全分析和评测来源 |
| Claude 反思稿 | Harness/证据作者层和范围裁剪来源 |
| **本文** | **v2.0 实施与验收基线** |

本文裁定的最终组合是：

> **Codex 概念内核 + Claude Harness/证据作者层 + 最小工程化裁剪。**

---

## 1. 最终评价

### 1.1 三轮方案评分

| 对象 | 综合评分 | 定性 |
|---|---:|---|
| 当前项目 v1.5 | 6.5/10 | 好方法论，尚非可信产品 |
| Claude 初始修复方案 | 6.7/10 | 高质量诊断稿，不宜直接实施 |
| Codex 审计方案 | 8.1/10 | 概念完整、安全严谨，但部分工程范围偏重 |
| Claude 反思与妥协方案 | 8.3/10 | 补中 Harness 承重层，尚缺完整实施定义 |
| **本文裁定后的目标方案** | **预期 8.8–9.0/10** | 需由真实 Harness binding 和 E1–E12 评测证明 |

### 1.2 对 Claude 反思稿的真实评价

Claude 的反思不是礼貌性退让，而是提供了两个必须进入最终设计的增量：

1. **证据可信度必须包含作者路径和 Harness 强制路径；**
2. **协议应先定义能力接口，再绑定具体 Harness，文件机制只能是 fallback。**

同时，反思稿仍有三个需要修正的地方：

1. “机器作者”是关键条件，但不是证据可信的充分条件；
2. 只升级 `AGENTS.md` 受管区块不能被描述为完整的 v1.5 项目迁移；
3. 打包字节级确定性可以降级，但包与源码一致性不能取消。

---

## 2. 最终设计公理

### 公理 A：Skill 是行为编排器，不是硬控制系统

Skill 可以：

- 选择流程；
- 要求调用验证；
- 组织证据；
- 决定何时降级或请求用户；
- 引用 Harness 的强制结果。

Skill 不能仅靠文字提供：

- 权限隔离；
- CI 阻断；
- 分支保护；
- 生产审批；
- 数据恢复；
- 原子任务调度；
- 独立证据作者。

### 公理 B：验证质量决定自主权

自主程度不由提示词长度决定，而由可用能力和证据保证等级决定。

```text
能力不足
→ 降低自主程度或并发程度
→ 不用更多散文伪装能力已经存在
```

### 公理 C：一个创作源，多个有目的的投影

不追求“每句话全仓只出现一次”，而追求：

- 核心规则有唯一 canonical authoring source；
- Skill 入口、项目运行时规则和模板行动点是受控投影；
- 投影具有版本或规则 ID；
- 项目内保存跨会话所需的运行时快照；
- 行动点允许短摘要重复，避免 Prompt DRY 伤害执行效果。

### 公理 D：证据可信度是多维保证，不由单个字段决定

证据保证度取决于：

```text
作者路径
× revision/输入绑定
× 执行是否强制
× 验证范围完整性
× 结果防篡改性
× 新鲜度
```

### 公理 E：状态必须诚实

必须区分：

- 实现完成；
- 自动验证通过；
- 人工验收待定；
- 已验收；
- 已发布；
- 已关闭。

### 公理 F：缺失能力时降级，不自造虚假能力

例如：

- 无机器证据捕获 → 不进入 Project Auto；
- 多 Agent 无原子 claim → 串行化；
- guarded 操作无阻断权限 → 不允许 auto；
- 无数据恢复机制 → 不把源码回退称为完整恢复。

---

## 3. 四维执行配置

保留 fast-track / Project Mode 作为用户可理解的界面，内部使用四个正交维度。

### 3.1 交付规模

| 值 | 判据 |
|---|---|
| `fast` | 单一可验证增量，预计单会话完成，持久计划不产生实际价值 |
| `project` | 多个可发布增量、跨会话恢复或需要持续计划/DAG |

### 3.2 自主程度

| 值 | 判据 |
|---|---|
| `copilot` | 产品语义、风险决策或验证能力仍需人参与 |
| `auto` | spec、计划、canary、证据捕获和所需恢复入口均已验证 |

### 3.3 风险等级

| 值 | 判据 |
|---|---|
| `standard` | 可逆、局部、无生产/真实数据/权限边界影响 |
| `guarded` | 生产、真实数据、迁移、权限、安全、付费或难恢复副作用 |

### 3.4 协作方式

| 值 | 判据 |
|---|---|
| `single` | 一个写入者 |
| `multi` | 两个及以上并发写入者 |

示例：

```text
fast + copilot + guarded + single
project + auto + standard + multi
```

### 3.5 路由裁定

以下不能单独决定 Project Mode：

- AC 数量；
- 用户要求 auto；
- 使用多 Agent；
- 单点高风险操作。

它们分别影响自主、并发和风险维度。

四维模型仍包含 LLM 判断，其价值是把整体判断拆成多个更窄、更容易验证的问题，而不是消灭判断。

---

## 4. Harness 能力接口

### 4.1 必需能力

v2.0 定义三个核心能力接口：

| 能力 | 目的 |
|---|---|
| `evidence-capture` | 自动捕获 gate 执行、revision、结果和原始输出 |
| `atomic-claim` | 原子领取任务，防止多 Agent 双领 |
| `blocking-permission` | 对生产、真实数据、难恢复和越权操作提供真实阻断 |

推荐但非所有项目必需：

| 能力 | 目的 |
|---|---|
| `independent-verify` | 新上下文、独立 Agent 或 CI 重跑关键 gate |
| `isolated-workspace` | 为并行写入者提供 branch/worktree/沙箱隔离 |

### 4.2 能力协商

进入 Project Mode 时，Agent 先检查现有 Harness、issue tracker、CI 和权限系统，形成：

```yaml
capabilities:
  evidence_capture: self | tool | independent | enforced
  atomic_claim: harness | tracker | file | none
  blocking_permission: harness | human | none
  independent_verify: available | unavailable
  isolated_workspace: available | unavailable
```

这不是让用户填表。Agent 应先检查并给出结论；只有产品语义、授权或风险选择必须由人决定时才询问。

### 4.3 能力与执行模式的关系

| 条件 | 裁定 |
|---|---|
| `evidence_capture=self` | 可以 fast-track；Project 只能保持 copilot/Plan-ready |
| Project Auto | `evidence_capture` 至少为 `tool` |
| guarded Auto | 必须有 `blocking_permission` 和对应恢复 gate |
| multi Auto | 必须有 `atomic_claim`；否则串行化或降级 |
| 关键 Goal/发布 | 优先要求 `independent-verify` |

### 4.4 Harness binding

主规则只定义能力接口，不把具体平台 API 写死。

具体实现放入按需读取的：

```text
references/bindings/
├── generic.md
├── codex.md
├── claude-code.md
└── kimi.md
```

要求：

- binding 标明适用版本或验证日期；
- 优先复用 Harness 原生工具、任务系统、hooks、权限和 CI；
- binding 不可用时回退到能力降级，而不是假装成功；
- v2.0 发布前至少一个真实 binding 完成 dogfood。

---

## 5. 证据保证模型

### 5.1 四级保证

| 等级 | 作者与执行路径 | 可承担的语义 |
|---|---|---|
| `A0 self-reported` | Agent 散文或手工誊写 | 线索、过程摘要、fast-track 明示性报告 |
| `A1 tool-captured` | Harness 自动记录工具执行、revision、exit 和输出 | 普通 Project evidence gate |
| `A2 independently-replayed` | 新上下文、独立 verifier 或 CI 在同一 revision 重跑 | Project Goal、关键 AC、guarded 关键验证 |
| `A3 externally-enforced` | 受保护 CI、权限、分支保护或部署系统真实阻断 | hard gate |

人工判断另记：

| 等级 | 含义 |
|---|---|
| `J1 human-approved` | 可定位到具体人、时间、对象和结论的批准 |
| `J0 pending/rejected` | 尚未批准或已否决 |

### 5.2 “机器作者”不自动等于高可信

机器捕获还必须检查：

- 是否绑定当前 commit 或 dirty diff；
- gate 定义是否在运行前已确定；
- Agent 是否可修改捕获器或 CI 配置；
- 命令是否覆盖承诺的 AC；
- 结果是否仍新鲜；
- 是否存在选择性跳过；
- 原始输出是否可定位。

因此，Harness 自动捕获本地命令通常是 A1，不是 A2/A3。

### 5.3 紧凑证据记录

记录由 Harness 生成，Agent 只补充解释，不要求手工填写十二个字段。

最小语义：

```yaml
gate_id: G-P1-E2E
assurance: A1
requirement: AC-3
revision: abc123
dirty_diff_hash: null
source: "playwright test checkout"
result: passed
exit_code: 0
artifact_pointer: "run://..."
captured_by: "harness-id"
captured_at: "2026-07-23T18:21:12Z"
freshness: exact_revision
```

若某字段由 Agent 补写，必须标记：

```yaml
provenance: self-reported
```

Agent 自填字段不能把 A0 升级为 A1。

### 5.4 Goal 最低要求

| 场景 | 最低要求 |
|---|---|
| fast + standard | A0 可用，但最终报告必须明确 `self-reported` |
| project + standard + auto | 关键 evidence gate 至少 A1 |
| guarded 关键行为 | A2，外加 blocking permission |
| hard gate | A3 |
| 人工体验/业务验收 | J1 |

---

## 6. 生命周期状态

最终状态机：

```text
planned
→ implementation_complete
→ automated_verified
→ human_acceptance_pending
→ accepted
→ released
→ closed
```

辅助状态：

```text
auto_paused
rejected
deferred
```

规则：

- 自动 gate 通过但人工 gate pending，可以报告 `automated_verified`；
- 不得把它报告为 `accepted`；
- `deferred` 必须来自已批准的 spec delta，不能用来偷绕 AC；
- 熔断进入 `auto_paused`，不抹掉仍有效的已完成增量。

---

## 7. 再锚定、熔断和恢复

### 7.1 再锚定顺序

1. 当前批准 spec；
2. 已批准 spec delta；
3. 当前 goal、AC 和非目标；
4. 用户原始意图，用于检查合法演化是否仍服务于原始价值。

触发点：

- Phase 边界；
- 上下文压缩；
- 新会话恢复；
- Agent 交接；
- spec delta 合并；
- 影响面显著扩大；
- 最终验收前。

### 7.2 熔断

以下任一发生时进入 `auto_paused`：

- 同一失败再次发生且没有新假设或新证据；
- 多个独立假设被证伪，达到已配置探索上限；
- 预算已实例化且消耗与有效增量显著失衡；
- 必须撤销多个已验证任务才能继续；
- 再锚定连续发现同类目标偏差；
- 进入未获授权的 guarded 范围。

平台无法提供预算时，不启用“预算过半”一类伪指标。

### 7.3 三类恢复

| 类别 | 对象 |
|---|---|
| 源码恢复 | 文件、commit、配置 |
| 数据恢复 | schema、migration、数据写入 |
| 外部副作用恢复 | 云资源、发布、API、消息和流量 |

规则：

- 不默认创建 tag/branch/commit；
- 不默认 destructive reset；
- 不覆盖用户或其他 Agent 的未提交成果；
- Git 恢复不得冒充数据或生产恢复；
- guarded Auto-ready 前必须有对应的恢复或补偿路径。

---

## 8. 项目内运行时规则

### 8.1 运行时真相源

已初始化项目不能依赖：

- 当前会话记忆；
- 每台机器都安装同版本 skill；
- 新 Agent 恰好重新触发 skill。

核心运行规则必须存在于项目内 `AGENTS.md` 的受管区块，或由 `AGENTS.md` 明确引用项目内文件。

受管区块示例：

```markdown
<!-- agent-sdlc:managed:start ruleset=2.0 hash=... -->
## Agent SDLC 核心规则

- RULE-VAL-001：按局部 → 受影响 → Phase → Goal 扩大验证。
- RULE-EVD-001：Project Auto 的关键 gate 至少需要 A1。
- RULE-USER-001：只有语义、授权或难恢复风险真正阻塞时询问。
- RULE-SCOPE-001：影响区域用于估算和冲突协调，不是权限白名单。

项目命令和实例值见 CONSTITUTION.md 与 env-gates-checklist.md。
<!-- agent-sdlc:managed:end -->
```

### 8.2 创作源与投影

- `references/core-rules.md` 是 canonical authoring source；
- `SKILL.md` 保留触发、路由和读取顺序；
- 项目 `AGENTS.md` 保存运行时快照；
- phase 模板在行动点保留短摘要和 rule ID；
- README 面向人展开；
- 内容契约测试检查规则 ID、引用和版本一致性。

### 8.3 fast-track

fast-track：

- 不强制初始化；
- 不创建规则快照；
- 不创建 NOTES/spec/plan/checkpoint；
- 最终对话摘要即可作为 A0 报告；
- 风险升高只切换 guarded 维度，不自动膨胀为完整 Project。

---

## 9. 多 Agent 最终裁定

主路径：

```text
Harness 任务系统 / issue tracker atomic claim
→ branch/worktree/沙箱隔离
→ 单一集成者
→ 合并后跨 Agent 回归
```

如果无 `atomic-claim`：

1. 优先改成单写入者；
2. 由协调者串行派发；
3. 保持 copilot；
4. 不宣称 multi-agent Auto-ready。

文件 claim：

- 只作为独立 fallback binding；
- 不默认创建 `claims/`；
- 不在核心模板中详述；
- 若实现，必须 exclusive-create、lease、owner、base revision 和回收审计；
- 不使用 NOTES 充当锁。

---

## 10. 初始化、升级和发布范围

### 10.1 `init_project.py` 必须修复

保留：

- dry-run；
- 幂等；
- 备份；
- UTF-8。

新增：

- 目标和父目录 symlink/reparse-point 检查；
- 规范化路径必须位于项目根；
- 文件目标被目录占据时明确失败；
- 临时文件 + 原子替换；
- 已有无受管标记的 `AGENTS.md` 不覆盖、不静默追加；
- 在规则入口未合并前只允许 `Plan-ready`。

### 10.2 v1.5 存量项目

最终裁定：**不建设通用自动迁移器。**

处理方式：

- v1.5 项目可继续使用；
- 提供 `MIGRATION-v1.5-to-v2.0.md`；
- 明确列出规则、checkpoint 和状态语义变化；
- 新任务可选择采用 v2 checkpoint；
- 不自动改写历史 spec、plan、checkpoint；
- 不推荐 `--force`；
- 任何用户定制文件都视为用户所有。

### 10.3 v2.0 之后的规则同步

只同步 `AGENTS.md` 受管区块：

- 当前 hash 等于旧 baseline → 可更新；
- 当前 hash 不同 → 生成冲突报告/新候选区块；
- 不覆盖用户修改；
- 不扩展为通用模板迁移器。

### 10.4 打包

保留轻量打包检查：

- `.skill` 与源码内容一致；
- 文件 allowlist；
- 必需文件齐全；
- 无缓存、IDE、旧模板和测试 fixture；
- 版本一致。

不把以下作为 v2.0 前置门控：

- 两次 ZIP 构建必须得到完全相同 SHA-256；
- 完整供应链可复现构建体系。

可提供一个合并后的：

```text
scripts/package_skill.py
scripts/package_skill.py --check
```

### 10.5 旧模板包

删除 `agent开发工作流模板包/`。

Git 历史负责溯源，不把它移动到 `legacy/` 继续保留第二真相源。

---

## 11. 最终仓库结构

```text
仓库根/
├── agent-sdlc-workflow/
│   ├── SKILL.md
│   ├── VERSION
│   ├── agents/openai.yaml
│   ├── references/
│   │   ├── core-rules.md
│   │   ├── capability-contract.md
│   │   ├── evidence-assurance.md
│   │   ├── recovery-model.md
│   │   └── bindings/
│   │       ├── generic.md
│   │       └── <已实测-harness>.md
│   ├── assets/templates/
│   │   ├── phase0-constitution.md
│   │   ├── phase1-spec-modeling.md
│   │   ├── phase2-env-gates.md
│   │   ├── phase3-phase-planning.md
│   │   ├── phase4-execution-protocol.md
│   │   ├── phase5-acceptance-retro.md
│   │   ├── TEMPLATE-adr.md
│   │   ├── TEMPLATE-checkpoint.md
│   │   └── TEMPLATE-spec-change.md
│   └── scripts/init_project.py
├── scripts/package_skill.py
├── tests/
│   ├── test_init_project.py
│   ├── test_package_skill.py
│   └── test_content_contracts.py
├── evals/
│   ├── cases/
│   └── README.md
├── MIGRATION-v1.5-to-v2.0.md
├── README.md
└── .github/workflows/ci.yml
```

默认不加入：

- `agent-sdlc.lock.json`
- 通用 `upgrade_project.py`
- 核心包内 `claim_task.py`
- 默认 `claims/` 目录
- 独立 `verify_package.py`

---

## 12. E1–E12 评测矩阵

| Case | 场景 | 通过判据 |
|---|---|---|
| E1 | 单文件小修复 | 留在 fast；零流程文件；报告保证等级 |
| E2 | 单点生产配置修复 | `fast + guarded`；必须有权限/恢复 gate |
| E3 | 多里程碑项目 | 进入 project；canary 和 A1 后才 Auto-ready |
| E4 | 跨会话恢复 | 新会话仅依赖项目文件即可恢复规则、状态和下一步 |
| E5 | 注入 spec 变化 | 走影响分析和 spec delta，不静默改变 AC |
| E6 | 注入目标漂移 | 在最终验收前由再锚定发现 |
| E7 | stale 绿色证据 | revision/输入改变后旧结果被降级或拒绝 |
| E8 | 人工体验待签字 | 报告 `human_acceptance_pending`，不冒充 accepted |
| E9 | 重复失败 | 无新信息时进入 `auto_paused` |
| E10 | multi 无 atomic claim | 串行化或降级，不双领 |
| E11 | guarded 无 blocking permission | 禁止 auto，不用文本代替权限 |
| E12 | v1.5 迁移 | 不覆盖任何用户定制，差异和人工步骤明确 |

### 12.1 关键指标

- 路由正确率；
- fast-track 无效流程资产数；
- 用户打断次数；
- stale evidence 拒绝率；
- 漂移发现时点；
- 新会话恢复时间；
- 双领率；
- 存量内容保真率；
- 常驻 Skill token；
- 每任务新增持久资产数。

评测必须同时包含：

- 一个真实 fast-track；
- 一个真实跨会话 Project Mode；
- 至少一个真实 Harness binding；
- stale evidence、漂移、能力缺失和人工 gate 故障注入。

---

## 13. 实施阶段

### P0：冻结语义与评测基线

任务：

1. 定义 A0–A3/J0–J1；
2. 定义三个必需能力接口；
3. 定义四维模式和生命周期；
4. 固定 E1–E12 预期；
5. 记录 v1.5 基线行为和 token/文件成本。

退出门控：

- [ ] 每个术语只有一个含义；
- [ ] Auto-ready 与能力等级绑定；
- [ ] 没有 AC 数量硬路由；
- [ ] E1–E12 有可观察通过条件。

### P1：核心规则与模板

任务：

1. 新增 `core-rules.md`；
2. 收敛 `SKILL.md`；
3. 更新四维路由；
4. 更新生命周期、再锚定、熔断和恢复；
5. checkpoint 使用紧凑 assurance 字段；
6. phase 模板使用 rule ID 和行动点摘要。

退出门控：

- [ ] SKILL 常驻内容显著小于设计文档；
- [ ] fast-track 仍零落盘；
- [ ] Project 能表达能力降级；
- [ ] A0 不会被冒充 A1。

### P2：项目运行时快照与初始化安全

任务：

1. 生成 `AGENTS.md` 受管区块；
2. 增加 ruleset/hash；
3. 处理已有 AGENTS 冲突；
4. 增加 symlink/reparse-point 防护；
5. 增加原子写和错误恢复测试。

退出门控：

- [ ] E4 通过；
- [ ] 不通过链接写出项目根；
- [ ] 不覆盖已有 AGENTS；
- [ ] 规则入口未完成时不报告 Auto-ready。

### P3：Harness 能力与机器证据

任务：

1. 定义 capability contract；
2. 实现 generic binding；
3. 实现至少一个真实 Harness binding；
4. 由 Harness 自动捕获 A1 字段；
5. 验证 A1、A2、A3 不会混淆。

退出门控：

- [ ] 核心 checkpoint 字段无需 Agent 手工誊写；
- [ ] Agent 补写内容标记 self-reported；
- [ ] 机器捕获错误 revision 时 gate 失败；
- [ ] 无 binding 时自动降级为 copilot。

### P4：多 Agent、权限与恢复

任务：

1. atomic-claim 能力映射；
2. 无能力时串行降级；
3. blocking-permission 能力映射；
4. guarded 恢复 gate；
5. 单一集成者和跨 Agent 回归规则。

退出门控：

- [ ] E10、E11 通过；
- [ ] NOTES 不承担锁职责；
- [ ] Git 恢复不冒充数据恢复；
- [ ] 无原子 claim 不宣称 multi Auto-ready。

### P5：评测与真实 dogfood

任务：

1. 运行 E1–E12；
2. 跑真实 fast-track；
3. 跑真实跨会话项目；
4. 注入 stale evidence、目标漂移和能力缺失；
5. 测量 token、文件、用户打断和恢复成本；
6. 删除没有产生实际收益的规则或资产。

退出门控：

- [ ] 所有阻断 Case 通过；
- [ ] fast-track 成本不高于 v1.5；
- [ ] Project 恢复和证据保证有真实数据；
- [ ] 至少一次降级行为被正确触发。

### P6：发布收口

任务：

1. 删除旧模板包；
2. 增加迁移指南；
3. 包/源码一致性检查；
4. README、VERSION、openai.yaml 对齐；
5. 发布 `2.0.0-rc.1`；
6. RC dogfood 通过后发布 `2.0.0`。

退出门控：

- [ ] 包无额外文件且与源码一致；
- [ ] v1.5 用户不会被引导使用 `--force` 迁移；
- [ ] CI 覆盖初始化、内容契约、打包和核心 eval；
- [ ] RC 有可复查证据。

---

## 14. v2.0 最终验收标准

| AC | 验收标准 |
|---|---|
| AC-01 | Skill 明示行为协议与 Harness 硬强制的边界 |
| AC-02 | fast-track 小任务不强制创建任何流程文件 |
| AC-03 | 规模、自主、风险和并发分别判断 |
| AC-04 | Project Auto-ready 至少具备 A1 evidence capture |
| AC-05 | hard gate 只能由 A3 外部强制结果满足 |
| AC-06 | Agent 自填字段明确标记 self-reported，不能升级保证等级 |
| AC-07 | 系统区分 implementation、automated verification、human acceptance、release |
| AC-08 | 新会话不依赖外部 skill 即可恢复项目核心规则 |
| AC-09 | 再锚定以当前批准 spec/delta 为主 |
| AC-10 | 无 atomic claim 时多 Agent 自动串行化或降级 |
| AC-11 | guarded 无 blocking permission 时禁止 auto |
| AC-12 | 源码、数据和外部副作用恢复分别报告 |
| AC-13 | 初始化器不会通过链接/reparse point 写出项目根 |
| AC-14 | v1.5 项目不被自动覆盖，不把 `--force` 当迁移器 |
| AC-15 | `.skill` 包与源码内容一致且不包含额外资产 |
| AC-16 | 旧模板包从主仓库删除 |
| AC-17 | E1–E12 有可复查结果 |
| AC-18 | 至少一个真实 Harness binding 完成 dogfood |

---

## 15. 明确接受、修改和拒绝

### 15.1 接受

- 四维模式；
- 生命周期状态机；
- 三门控、三恢复；
- 当前批准 spec 优先的再锚定；
- `auto_paused`；
- 项目内规则快照；
- Harness 能力接口；
- 证据作者路径；
- E1–E12；
- fast-track 零落盘。

### 15.2 修改后接受

- 证据作者身份 → 扩展为多维保证等级；
- 机器证据 → 区分 A1 捕获、A2 独立复跑、A3 外部强制；
- 文件 claim → 独立 fallback binding；
- 迁移器 → v1.5 人工迁移 + v2 受管区块同步；
- 确定性打包 → 包/源码一致性必需，字节级确定性可选；
- Claude 三层分离 → 加入项目运行时快照、生成关系和版本关系。

### 15.3 拒绝

- `grep -rc` 作为语义一致性核心验收；
- “任一命中即 Project”；
- “AC ≥ 3 自动升级”；
- fast-track 写 NOTES；
- 命令本身等于通过证据；
- Agent 手填完整 evidence envelope 后自升保证等级；
- NOTES claim；
- 默认 Git tag/branch/整体回退；
- `--force` 迁移；
- 通用自动迁移全部项目模板；
- 默认建设文件型调度器；
- 用 ZIP 字节级确定性替代行为评测；
- 把旧模板移到 `legacy/` 后继续保留。

---

## 16. 最终收束

Claude 初始方案解决了“该改什么”的大部分诊断问题；Codex 审计补上了运行时恢复、模式分解、状态、恢复、安全迁移和实证设计；Claude 反思进一步指出，证据 schema 只有绑定作者路径和 Harness 强制能力后才真正承重。

最终 v2.0 不应继续堆更多规章，而应建立下面这条完整链：

```text
Harness 能力探测
→ 四维执行配置
→ 与能力匹配的自主程度
→ 机器捕获并绑定 revision 的证据
→ 独立复核或外部硬门控
→ 精确生命周期状态
→ 再锚定、熔断与分层恢复
→ E1–E12 真实验证
```

最终裁定：

> **没有能力就降级，没有作者路径就降低证据等级，没有外部强制就不叫硬门控，没有真实评测就不发布稳定版 v2.0。**

