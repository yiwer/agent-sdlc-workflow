# agent-sdlc-workflow 修复方案（v1.5 → v2.0）

- **状态**：提案，未落地（本文档仅为设计稿，未改动任何 skill 文件）
- **日期**：2026-07-23
- **适用范围**：`agent-sdlc-workflow/`（SKILL.md、六个阶段模板、三个资产模板、`scripts/init_project.py`）、`README.md`、打包件 `agent-sdlc-workflow.skill`、仓库级旧模板包 `agent开发工作流模板包/`

---

## 一、背景与诊断摘要

本 skill 的核心命题——**「验证决定自主权」**（Agent 的自主程度取决于反馈回路的可靠程度，而非更长的提示词）——是真正原创且承重的洞见；由此派生的 `Plan-ready` vs `Auto-ready` 区分（有计划 ≠ 可自治，只有反馈闭环被实测跑通才移交 auto）是同类框架中少见的非显然判断。这些是修复要**保留并发扬**的部分。

但存在四个结构性裂缝，是本方案要修复的对象：

1. **修辞是「控制」，机制是「自证」**。通篇使用「门控 / 退出门控 / 以上全部满足才报告完成」的强制修辞，实际却是一套全靠 Agent 自我签字的荣誉系统。每一个门控都是 Agent 替自己打的勾，checkpoint「验证证据」表允许纯散文自证，没有任何环节独立捕获或校验证据。
2. **自我背叛**：一份宣扬「最小落盘 / 选最轻流程 / 不为合规生产流程资产」的 skill，自身却是最大的冗余台账——同一条规则被近乎逐字复制 6–9 遍。
3. **模式路由脆弱**：全系统杠杆最大的决策（fast-track vs Project Mode）由一段模糊散文决定，后果不对称，且模式一旦选定即粘滞，无中途升降级纠错。
4. **长程自治的真实失败模式未被防御**：核心风险不是「测试没跑」而是「目标漂移 + 误差累积」（高效地构建了错误的东西）；而最锋利的自省问题被放在最末尾的 phase5 复盘，来得太晚。

另有若干缺失项：无回滚/恢复协议、无成本/预算熔断、多 Agent 故事单薄、happy-path-only（无人否决协议）、完成判据可被自欺。

---

## 二、基线冗余数据（grep 实测，仅统计现役 `agent-sdlc-workflow/`）

| 规则签名 | 出现次数 | 分布 |
|---|---|---|
| 升级触发清单（「共享契约…」） | **7** | SKILL×2、init_project.py×1、phase0/2/3/4 各 1 |
| 绿色证据复用 | **9** | SKILL×3、phase0/4/5 各 2 |
| 询问用户边界 | **7** | SKILL×2、init_project.py×1、phase0/3 各 1、phase4×2 |
| 影响区域 ≠ 白名单 | **4** | SKILL、init_project.py、phase3、phase4 |

两个进入修复清单的事实：

1. **冗余也焊在 `init_project.py` 的 `AGENTS_MD` 字符串里**——去重必须改 Python，不只改 md。
2. **仓库里还躺着第二套模板 `agent开发工作流模板包/`（模板 0–5）**，是 skill 前身，构成仓库级第二真相源。

---

## 三、统摄全部修复的两条公理

先立公理，再谈具体补丁。所有改动都从这两条推出。

### 公理 A — 三层分离（治冗余的根，而非逐句删）

| 层 | 归属 | 内容 | 权威来源 | 在全 skill 的份数 |
|---|---|---|---|---|
| **规则层** Rule | skill | 跨项目通用行为规则（节奏/边界/语义/范围） | `SKILL.md` | **恰好 1** |
| **实例层** Instance | 项目 | 规则在本项目的落地值（具体命令、通过标准、实测耗时） | `CONSTITUTION.md` / `env-gates-checklist.md` | 每项目 1（被填写，非复制） |
| **脚手架层** Scaffold | 模板 | 产出实例的薄骨架 + 一行指向规则的锚点 | 各 `phaseX.md` / 模板 | 每模板一行锚点 |

> 当前病灶：规则层文本被复印进脚手架层与实例层。修复动作 = 删掉复印件，换成「一行锚点 + 指针」。
> 判据：**写任何一句话前先问它属于哪层；规则只在 SKILL.md 出现一次。**

### 公理 B — 诚实强制（治「自证门控」的根）

在 SKILL.md 顶部声明 skill 的约束性质，并把「门控」强制三分：

```text
## 本 skill 的约束性质（先读这段）
这是行为协议，不是硬控制系统：Agent 读到的文字只能自我约束，硬边界来自
harness 权限、hooks、CI/部署与版本控制。凡能交给它们强制的，不靠本 skill 自律。
「门控」分三类，不得混淆：
- 硬门控：由 CI/hook/权限强制，本 skill 只引用其结果，不自证；
- 证据门控：可自跑，但必须产出可由第三方（新会话/用户/CI）复跑的指针；
- 判断门控：确需人签字（如人工体验验收），显式标出，不以自证代替。
```

> 这两条公理是修复一~六的共同地基。**必须先落地公理 A 的 canonical 块与公理 B 的声明，才能开始去重与机制对齐**——否则模板没有可指向的权威目标。

---

## 四、修复一：去重 + 立 canonical（对应诊断 #2）

### 4.1 在 SKILL.md 增设唯一权威定义块

把散落规则收敛为 SKILL.md 里一个 `## 核心定义` 节（其余一切文件只按名引用）。建议文本（示意，未落地）：

```markdown
## 核心定义（全 skill 唯一权威；其余文件按名引用，不复述）

### 验证节奏
- 局部：连贯改动后跑最短检查，不在每次编辑后回测。
- 受影响回归：任务/批次结束。
- Phase：相关集成、共享契约、关键路径 E2E。
- Goal：最后一次代码变更后的完整/发布级绿色证据；上层已等同或覆盖则复用。
- 提前升级：共享契约、schema、权限、安全、依赖、全局配置、并发、迁移或
  基础设施变化，或局部失败显示影响面扩大。
- 复用：代码与验证输入未变可引用最近绿色证据；相同失败不再给新信息时先换假设。

### 用户介入边界（且无其他安全任务可继续时才合并询问）
- 目标/非目标/AC 必须改变；新外部权限/付费服务/生产或真实数据；
  破坏性、难恢复或超出已授权工作区。
- 其余（普通失败、漏列文件、等价重构、实现选择、首次诊断）由 Agent 自判。

### 影响区域语义
任务卡「影响区域」仅用于估算与多 Agent 写冲突协调，不是权限白名单；
漏列文件直接改，checkpoint 概括重要影响，逐文件差异由 git 提供。

### 自主范围
当前 goal 内的实现/代码组织/测试组合/可逆取舍由 Agent 判断，不逐文件审批；
硬安全边界由 harness 权限/hooks/CI/部署承担，本 skill 不复制。
```

### 4.2 每个重复点的具体处置

| 当前位置 | 现状 | 改为 |
|---|---|---|
| `phase4` §验证节奏（5 行全文复述） | 复印规则 | 一行锚点：`按 SKILL.md「验证节奏」执行；本项目各级命令与通过标准见 CONSTITUTION.md。` |
| `phase0` §验证节奏 + §验证命令表 | 规则+实例混排 | 删规则复述；**保留命令表**（被填写的实例层，合法） |
| `phase2` §自动反馈命令表 + 升级触发段 | 规则+实例混排 | 保留命令表（实例）；升级触发段收成一行锚点指 SKILL.md |
| `phase3` 任务卡「回归升级条件」字段 | 复印触发清单 | 字段保留但改为：`无 / 命中 SKILL.md「提前升级」清单中的：__` |
| `phase5` §一致性检查 | 部分复述 | 改为对照「验证节奏」Goal 条的核对项，不复述定义 |
| `init_project.py::AGENTS_MD`（含节奏/触发/白名单/询问 4 条复印） | 焊在 Python 里的复印 | 瘦身为指针：命令表 + 「治理见 CONSTITUTION.md；规则见 skill 核心定义」 |
| `README.md` | 面向人的说明 | **保留**（README 给人读，非 agent 上下文，不计入冗余预算），但与 SKILL.md 措辞对齐 |

### 4.3 目标度量（修复成功的硬指标）

| 签名 | 现状（现役 skill） | 目标 |
|---|---|---|
| 「共享契约…」触发清单 | 7 | **1**（SKILL.md）+ 每项目实例按需展开 |
| 绿色证据复用 | 9 | **1**（SKILL.md）+ phase5 作为核对项引用 |
| 询问边界 | 7 | **1**（SKILL.md）+ phase4 一行锚点 |
| 影响区域 ≠ 白名单 | 4 | **1**（SKILL.md）+ phase3 任务卡一行 |

验收命令（修复后跑，应逼近目标）：

```bash
grep -rc "共享契约" agent-sdlc-workflow/   # 期望 1（仅 SKILL.md）
grep -rc "绿色"     agent-sdlc-workflow/   # 期望 ≤2（SKILL.md + phase5 引用）
```

### 4.4 必须正视的权衡

canonical 块放进**常驻**的 SKILL.md，意味着这几条规则从「按需加载于各模板」变为「始终在上下文」。这是**有意的取舍**：它们是承重规则，值得常驻；代价是常驻 token 略增、模板大幅瘦身、总包体下降、一致性得到单点保证。模板不再需要「独立可读的全文」，只需「一行锚点 + 指针」——因为 SKILL.md 随 skill 重调而重载、CONSTITUTION.md 是跨会话恢复的输入，resume 路径不缺信息。

### 4.5 仓库级去重：处置 `agent开发工作流模板包/`

它是 skill 前身（模板 0–5），构成第二真相源。三选一，建议 **A**：

- **A（推荐）** 移出到 `legacy/` 并在 README 注明「已并入 agent-sdlc-workflow，仅供溯源」；
- **B** 保留但加「已废弃，以 agent-sdlc-workflow/ 为准」的显著标记；
- **C** 删除（若 git 历史已足够溯源）。

---

## 五、修复二：修辞与机制对齐（对应诊断 #1）

### 5.1 checkpoint 证据表升级——让「可复现」结构化

现状三列 `命令/检查 | 结果 | 证据路径` 允许纯散文自证。改为强制分类 + 可复现指针：

```markdown
## 验证证据
| 门控 | 类型(硬/证据/判断) | 命令或来源 | 可复现指针 | 结果 |
|---|---|---|---|---|
| 局部检查 | 证据 | `pytest -k auth` | 输出@commit abc / CI #123 | pass |
| 关键路径 | 证据 | `playwright test checkout` | transcript: .verify/0723.md | pass |
| 体验验收 | 判断 | 人工 | 待用户签字 | pending |
```

规则：**证据门控的「可复现指针」不得是散文**——必须是 CI URL、会话/输出路径、或一条他人可重跑的命令（最后者是无 CI 时的通用底线）。判断门控结果只能是 `pending/已签字`，禁止以 `pass` 自证。

### 5.2 最终验收引入「无记忆复核者」

phase5 增加（跨 harness 以「强烈建议」而非强制落地）：

```markdown
## 独立复核（强烈建议）
最终验收尽量由一个无实现记忆的新上下文执行：新会话或 review/verify 调用，
只读 AC + 最终 diff + 重跑门控，不看实现过程的挣扎。
理由：执行者对「它完成了」有沉没成本偏见；无记忆复核者没有。
无此条件时，至少确保每个证据门控的指针可被他人/CI 独立复跑。
```

### 5.3 完成判据去自欺

phase4 §Goal 完成 / phase5 §关闭门控 增加硬字段：完成报告必须**逐条 AC 列出可复现指针**，并显式枚举仍处于 `pending` 的判断门控。规则：**存在任何 pending 判断门控时，禁止报告 goal 完成**——把「人工体验验收被明确标出」从一句散文期望升级为阻断式字段。

---

## 六、修复三：模式路由 + 再锚定 + 熔断 + 回退（对应诊断 #3、#4 及缺失项）

### 6.1 模式判定从「模糊散文」改为「可复访的清单」

SKILL.md §选择最轻的充分流程 改为可勾选、且结果落盘的判定（写入 NOTES 首行，故可复访、可升降级）：

```markdown
### 模式判定（开始时勾选，结果与理由写入 NOTES 首行，每个 Phase 边界复检）
任一为真 → Project Mode：
- [ ] 跨多个里程碑/多个可发布增量
- [ ] 预计跨会话（单会话装不下）
- [ ] 多 Agent 并发
- [ ] 用户显式要求长程 auto
- [ ] 涉及不可逆/高风险变更（迁移、数据、生产）
否则 → fast-track。
```

**升级触发（fast-track → Project）**：

```text
fast-track 中出现任一 → 停下升级，补 spec 与门控：
- AC 从 1 条膨胀到 ≥3 条且互相牵制；
- 需要改共享契约/schema/迁移；
- 已跨入第二个会话仍未完成；
- 开始需要协调另一个 Agent。
```

**降级触发（Project → fast-track）**：

```text
推进中发现实际增量退化为单点修改、无共享面影响、无跨会话 → 可降级；
已在手的 spec/checkpoint 保留为记录，不强制走完五阶段。
```

要点：**模式不再粘滞**——每个 Phase 边界 / checkpoint 复检一次判定。

### 6.2 再锚定检查——把 phase5 的好问题前移到执行循环（本方案最关键的新增）

phase4 §自主执行循环 中，**每个 Phase 门控通过后、进入下一 Phase 前**插入：

```markdown
## 再锚定检查（Phase 门控通过后必做）
对照原始 spec 与用户最初请求回答：
1. 目前的产出，是否仍在解决用户最初的那个问题？（目标漂移？）
2. 已实现的行为，与 spec 描述的是否仍是同一件事？（还是我们悄悄重定义了它？）
3. 剩余 Todo 是否仍服务于原始 AC？有无任务已变成「为做而做」？
4. 若用户此刻回来看，会不会说「这不是我要的」？
任一答「可能偏了」→ 暂停，走 spec delta 或请用户确认，再继续。
```

phase5 的复盘保留为轻量版终检，但承重点转移到这个在途仪表上。

### 6.3 熔断（补成本意识与零进展停止）

phase4 新增：

```markdown
## 熔断（出现任一，立即停 auto 并报告现状，不静默重试）
- 同一任务连续失败 ≥3 次且每次不再提供新信息（假设已穷举）；
- 预算过半（token/时间）但通过门控的增量远低于预期；
- 需要回退已完成的多个任务才能继续（累积误差信号）；
- 再锚定检查连续报警。
报告：已完成的 / 阻塞的 / 已试假设 / 建议下一步（继续/调整目标/回退/人工接管）。
```

### 6.4 回退协议（补缺失项，复用 git，零新机制）

phase4 新增：

```markdown
## 回退
- 每个 Phase 门控通过 = 一个「已知良好点」：在该点打 git tag/branch 或记录 commit。
- 需整体回退时，回到最近一个绿色门控点，而非逐文件手动撤销。
- spec delta 引发的回退：先在 delta 标注受影响任务与回退范围，再执行。
```

---

## 七、修复四：五维建模抗表演（对应诊断 #1 的表演风险）

phase1 §实体五维建模 顶部加「前置闸门」与反模式提示，削弱表格的形式重力：

```markdown
> 先回答：本项目是否真有领域实体值得建模？纯工具/脚本/薄封装 → 跳过五表，
> 写一句理由即可。
> 五表是探针不是交付物：填满≠想清楚；为凑表编造的属性/事件是负资产。
> 判据：这一行是否真约束实现或验证？若不影响任何 AC，删掉它。
```

并把「可溯源到 AC」从 Rules 表扩展到 Behaviors/Rules 两表（每行须能指到 AC，指不到即为删除候选）。模板各表加注 `无则留空并注理由`。

---

## 八、修复五：多 Agent 与「人否决」协议（补 happy-path-only 缺陷）

### 8.1 多 Agent 协作（phase4 §自主范围 扩充）

```markdown
## 多 Agent 协作
- 共享内存 = 文件：领取任务时在 NOTES 写一行 claim，释放时更新；冲突以 NOTES+git 为准。
- 交班产物 = 最新 checkpoint + NOTES + 一行「当前前沿」指针；
  接棒 Agent 读这些即可续跑，不依赖任何会话内记忆。
- 共享写点（migration/公共 schema/lock）仍由单 Agent 串行。
```

### 8.2 人否决路径（phase1/phase3 各加一条）

```markdown
## 人否决时
人否决 spec/ADR/计划 → Agent 把否决理由记入对应文件（ADR 走 rejected 分支）
→ 按人的方向修订 → 不重复提交已被否决的同一方案，除非带来新证据。
```

---

## 九、修复六：README / 版本 / 工具链同步

- README 与 SKILL.md 新措辞对齐（保留面向人的适度展开，不计冗余预算）。
- `init_project.py::AGENTS_MD` 按 4.2 瘦身；`COPY_MAP` 随模板改名同步；跑 `tests/test_init_project.py` 确认脚手架仍正确生成。
- 版本 **v1.5 → v2.0**（语义级变更：约束性质声明 + 门控三分 + checkpoint schema + 新协议）。
- 重新打包 `agent-sdlc-workflow.skill`。

---

## 十、落地顺序与依赖（DAG）

```text
P0 地基：公理A canonical 块 + 公理B 约束性质声明 + 门控三分   ──┐
   （SKILL.md）                                                │ 阻塞
   ├─ P1 去重：模板 + init_project.py 内嵌串 → 锚点化          ◄┘
   ├─ P2 机制对齐：checkpoint schema / 无记忆复核 / done 去自欺 ◄┘
P3 模式路由 + 再锚定 + 熔断 + 回退（phase3/phase4，与 P1/P2 可并行）
P4 五维抗表演 + 多Agent + 否决协议（phase1/phase3/phase4，独立）
P5 README 对齐 + v2.0 + 迁移说明 + 跑测试 + 处置旧模板包      （最后）
```

- **P0 阻塞 P1、P2**（没有权威目标就无法去重 / 分类门控）。
- **P3、P4 互相独立、且独立于 P1/P2**，可并行。
- **P5 收尾**。

---

## 十一、迁移与兼容性

| 对象 | 影响 | 处理 |
|---|---|---|
| **已初始化的存量项目** | init 幂等跳过已有文件，旧模板已被拷成项目文件 → 不受 skill 更新影响，继续可用 | 无需迁移；想换新薄模板者用 `--force`（自动备份到 `.agent-sdlc-backups/`） |
| **进行中的 checkpoint** | 旧三列格式 | 非破坏：旧记录照旧保留，新任务起用新 schema |
| **`.skill` 打包件** | 需重新打包 | P5 重打 |
| **旧模板包 `agent开发工作流模板包/`** | 第二真相源 | 按 4.5 建议移入 `legacy/` |

---

## 十二、风险与权衡（每项修复的副作用，须事先认账）

1. **去重后模板「变薄」可能让独立阅读体验下降**——缓解：每个锚点保留一句话语义摘要（非全文），如 `按 SKILL.md「验证节奏」（局部→受影响→Phase→Goal）`。
2. **canonical 常驻 SKILL.md 增加常驻 token**——已在 4.4 认账；以总包体下降与单点一致性对冲。
3. **门控三分 + 可复现指针提高合规成本**，小任务上可能显重——缓解：明确 fast-track 不强制 checkpoint schema，仅 Project Mode 的 Goal 门控强制。
4. **再锚定/熔断可能被 Agent 形式化勾选**（又是自证）——这是公理 B 的残余风险，只能靠「无记忆复核 + 可复现指针」把最关键项移出纯自证；无法根除，诚实标注。
5. **无记忆复核跨 harness 可用性不一**——故定为「强烈建议」，底线退化为「指针可被他人/CI 复跑」。

---

## 十三、如何验证修复本身成功（用 skill 自己的复盘问题反身检验）

| 维度 | 检查 | 通过判据 |
|---|---|---|
| 冗余 | `grep -rc "共享契约" agent-sdlc-workflow/` 等（见 4.3） | 各签名逼近目标份数 |
| 自证 | 全仓是否还有「结果」列无「可复现指针」的门控表 | 无 |
| 漂移防御 | phase4 是否含再锚定检查与熔断 | 有 |
| 路由 | SKILL.md 是否含可勾选模式判定 + 升降级触发 | 有 |
| **dogfood** | 用一个真实小项目跑 v2.0 全程，统计 token / 用户打断次数 / 是否真触发过再锚定或熔断 | 对应 phase5 复盘问题 2/3/4 有真实答案而非「无变更」 |

最后一项 dogfood 是最硬的验收：**让 skill 在自己的复盘问题上给出真实数据**，而不是又一次自证。

---

## 附录：要保留并发扬的既有优点（修复时不得破坏）

1. 「验证决定自主权」——核心洞见，放在最承重处。
2. `Plan-ready` / `Auto-ready` 区分——击中「有计划 ≠ 能自治」的要害。
3. 分层验证节奏 + 「别每次编辑后跑全套」——好的工程判断编纂。
4. 反表演姿态——「不为合规生产流程资产」「无新知识记『无变更』」「N/A 写理由而非静默跳过」。
5. 「影响区域不是权限白名单」——正确识别 git 已提供文件级审计，逐文件审批是剧场。
6. 最小落盘教义 + 以文档腐烂为头号敌人。
7. `init_project.py` 工程质量：幂等、dry-run、force+备份、有测试。
