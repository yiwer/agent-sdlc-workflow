# agent-sdlc-workflow 项目审计与详细修复方案

- **状态**：独立评审稿，待确认后实施
- **日期**：2026-07-23
- **审计基线**：`45e36b9`（`master`）
- **评审输入**：当前仓库、现役 skill、旧模板包、打包件、测试，以及 `修复方案-v2.0.md`
- **适用范围**：方法论、Skill 指令架构、脚手架、迁移、证据模型、长程自治、多 Agent、测试与发布

---

## 0. 执行摘要

### 0.1 一句话结论

当前项目是一个**有原创洞见、表达成熟、工程骨架合格的行为协议**，但还不是一个可信的 SDLC 控制系统：

- 方法论约 **9/10**；
- 工程产品约 **6/10**；
- 控制与强制能力约 **4/10**；
- 综合评分 **6.5/10**。

`修复方案-v2.0.md` 对“自证门控、规则冗余、模式粘滞、目标漂移”的诊断基本正确，但存在五个新的结构性问题：

1. 把代码世界的 DRY 原则过度套用到 LLM 指令系统；
2. 让项目内模板反向依赖外部 `SKILL.md`，破坏跨会话和跨工具恢复；
3. 用新的二元清单制造伪精确路由；
4. 把“可重跑命令”误当成“通过证据”；
5. 回滚、迁移和多 Agent 协议不足以安全落地。

因此，本审计建议：

- **保留**：约束性质声明、门控三分、再锚定、五维建模抗表演、无记忆复核；
- **重写**：canonical 架构、模式模型、证据模型、完成状态、熔断、回滚、多 Agent；
- **拒绝**：用 `--force` 迁移、用 NOTES claim 充当锁、默认 Git tag/整体回退、把旧模板仅移动到 `legacy/`、把所有规则机械压缩到唯一文本出现点。

### 0.2 推荐目标

v2.0 应从“更多协议文字”升级为四个可验证能力：

1. **项目内可恢复**：脱离当前会话和外部 skill 安装状态，项目仍能恢复核心规则；
2. **证据绑定版本**：每个 gate 结果都能回答“在哪个 revision、什么环境、由谁、用什么命令得到”；
3. **状态诚实**：区分实现完成、自动验证通过、人工验收待定、已验收/已发布；
4. **工具链自证**：有安全升级、确定性打包、CI 和真实场景评测。

---

## 1. 审计范围与实测事实

### 1.1 当前有效资产

现役 skill 包含：

- `SKILL.md`
- `agents/openai.yaml`
- 6 个阶段模板
- 3 个资产模板
- `scripts/init_project.py`

仓库还包含：

- 根目录 `README.md`
- 打包件 `agent-sdlc-workflow.skill`
- `tests/test_init_project.py`
- 前身模板包 `agent开发工作流模板包/`
- Claude 提案 `修复方案-v2.0.md`

### 1.2 已完成的只读/临时验证

审计期间确认：

1. `python -m unittest discover -s tests -v` 的 3 个测试全部通过；
2. `.skill` 包内 12 个文件与当前源码逐字节一致；
3. 当前仓库没有 CI workflow；
4. 当前仓库没有可重复打包脚本；
5. 当前仓库没有真实项目 eval、benchmark 或失败注入测试；
6. `agent开发工作流模板包/` 未被现役 skill 引用，但仍是可被人或 Agent 误读的第二套规则。

### 1.3 重复规则核验

在现役 `agent-sdlc-workflow/` 中：

| 签名 | 实测 | 结论 |
|---|---:|---|
| `共享契约` | 7 次 | Claude 的计数成立 |
| `影响区域` | 4 次 | Claude 的计数成立 |
| `验证节奏` 标题 | 3 处 | 存在明显重复 |
| `绿色` | 11 次原始命中 | “绿色证据复用 9 次”属于语义计数，不是稳定 grep 指标 |

`grep -rc` 会按文件输出计数，并不会天然给出一个全仓总数；用单个中文短语衡量语义冗余也容易误伤合法摘要。因此，去重验收不能只靠字符串次数。

---

## 2. 当前项目评分

> 评分标尺：10 分代表可以在真实、多会话、多 Agent 项目中低监护运行，并具备可复查证据。

| 维度 | 分数 | 评价 |
|---|---:|---|
| 核心洞见与原创性 | 9.2 | “验证决定自主权”和 `Plan-ready ≠ Auto-ready` 是真正承重的判断 |
| 产品定位与边界 | 7.8 | 双模式定位合理，明确反对普通小任务套完整流程 |
| 流程完整性 | 7.4 | 从需求、环境、规划、执行到验收的主链完整 |
| 低打扰与日常可用性 | 7.8 | 已经明显克制传统流程剧场 |
| 门控可信度 | 4.7 | 门控主要依赖执行者自报，缺少 revision 绑定和外部校验 |
| 长程自治与抗漂移 | 5.6 | 有 checkpoint/spec delta，但目标再锚定过晚 |
| 多 Agent 协作 | 4.0 | 有冲突意识，没有所有权、租约、集成者和失效恢复 |
| 脚手架工程质量 | 7.8 | 幂等、dry-run、备份、UTF-8 和基础测试表现良好 |
| 可维护性 | 5.8 | 规则重复、旧模板并存、包靠手工同步 |
| 测试与实证 | 4.5 | 测试只覆盖初始化器，没有验证工作流行为是否有效 |
| **加权综合** | **6.5** | **好方法论，尚未成为可信产品** |

### 2.1 最应保留的资产

1. **验证决定自主权**

   自主性不应由“提示词更长”决定，而应由反馈回路的可靠性决定。

2. **`Plan-ready` / `Auto-ready` 分离**

   有计划只代表“可以审阅”，只有本地反馈闭环和 canary 实测后才代表“可以长程自治”。

3. **分层验证节奏**

   局部、受影响、Phase、Goal 四层节奏兼顾速度和最终完整性。

4. **影响区域不是权限白名单**

   文件影响面服务于估算和冲突协调，版本控制负责文件级审计。

5. **最小落盘和反表演倾向**

   不为了证明遵守流程而生产 spec、checkpoint、ADR 和复盘资产。

6. **初始化器基本工程质量**

   默认幂等、`--dry-run`、`--force` 前备份和 UTF-8 处理都值得保留。

### 2.2 Claude 修复方案评分

| 维度 | 分数 | 评价 |
|---|---:|---|
| 根因诊断 | 8.8 | 自证门控、规则复制、模式粘滞和目标漂移基本命中 |
| 既有价值保护 | 8.5 | 明确保留验证节奏、Plan/Auto 分离和反表演原则 |
| canonical/去重架构 | 5.8 | 忽略项目运行时恢复，并把 Prompt 重复一概视为坏味道 |
| 证据与门控设计 | 6.7 | 门控三分正确，但复现配方和通过证据仍有混淆 |
| 模式路由 | 5.6 | 清单比散文可见，但触发条件武断且继续混合多个维度 |
| 漂移与成本防御 | 7.7 | 再锚定是最有价值的新增；熔断指标仍需实例化 |
| 多 Agent | 4.2 | NOTES claim、交接和串行写点不足以构成可靠并发协议 |
| 迁移与安全 | 4.3 | `--force` 迁移和 Git 回退建议存在用户数据/成果风险 |
| 验证与 dogfood | 6.5 | 有反身验证意识，但单个小项目不能覆盖核心场景 |
| **加权综合** | **6.7** | **高质量诊断稿，不宜直接作为实施 spec** |

若分开评价：

- 作为评审报告：**8.5/10**；
- 作为可以直接领取任务实施的修复设计：**约 6.2/10**。

---

## 3. 当前项目的结构性问题

### 3.1 强制修辞与实际能力不匹配

当前文本频繁使用：

- “退出门控”
- “全部满足才完成”
- “只有……才”
- “禁止……”

但 Skill 文字本身只能改变 Agent 行为倾向，不能提供：

- 权限隔离；
- 分支保护；
- CI 阻断；
- 部署审批；
- 真实数据保护；
- 独立证据捕获。

因此，当前项目不是“门控执行器”，而是“门控编排协议”。如果不先声明这一点，使用者会高估它的控制能力。

### 3.2 规则的创作源、运行时真相源和项目实例混在一起

当前相同规则同时存在于：

- `SKILL.md`
- 阶段模板
- 初始化器内嵌 `AGENTS_MD`
- README
- 旧模板包

这里实际混合了三种不同需求：

| 需求 | 应放位置 |
|---|---|
| Skill 被触发时的编排规则 | skill 内部 canonical source |
| 项目跨会话恢复时的运行规则 | 项目内 `AGENTS.md` 或等价入口 |
| 项目自己的命令、风险和决策 | `CONSTITUTION.md`、环境清单、spec、plan |

问题不是“同一句话出现多次”本身，而是这些副本没有明确生成关系、版本关系和读取场景。

### 3.3 二元模式混合了四个不同维度

当前 `fast-track / Project Mode` 同时承载：

- 交付规模；
- 会话时长；
- 自主程度；
- 风险等级；
- Agent 数量。

这些维度并不总是同向：

- 一个生产配置单点修复可能规模很小，但风险极高；
- 一个多 Agent 文档迁移可能范围较大，但安全风险很低；
- 一个单 Agent 项目也可能需要跨会话；
- 用户要求 auto 不代表需求天然需要完整 Project Mode。

二元模式可以保留为用户界面，但内部判定不能继续把这些维度混为一谈。

### 3.4 当前证据无法可靠绑定当前代码状态

现有 checkpoint 的三列：

```text
命令/检查 | 结果 | 证据路径
```

无法回答：

- 结果对应哪个 commit 或 dirty diff？
- 运行后是否又修改了代码？
- 使用了什么运行环境和依赖版本？
- exit code 是什么？
- 证据路径是否仍存在、是否对应当前 gate？
- 是执行者自报，还是 CI/其他上下文复核？

“有证据路径”不等于“证据与当前交付绑定”。

### 3.5 完成状态过于二元

当前语义主要是：

```text
未完成 → goal 完成
```

现实中至少需要区分：

1. 实现已完成；
2. 自动门控已通过；
3. 等待人工体验/业务签字；
4. 已验收；
5. 已发布；
6. 已关闭。

如果任何人工 gate pending 都禁止报告任何形式的完成，Agent 要么永远挂起，要么继续用模糊措辞绕过规则。

### 3.6 长程执行缺少在途目标校验

当前流程主要在最终复盘时才追问：

- 哪个假设最晚被证伪？
- 是否发生目标漂移？
- 哪些人工打断本可避免？

这适合复盘，不足以阻止在途漂移。长程失败最危险的形态不是测试红，而是测试一直绿、方向一直错。

### 3.7 回滚语义只覆盖源码，未覆盖真实副作用

仅记录 Git commit/tag 不能回滚：

- 数据库 migration；
- 已写入的真实数据；
- 云资源创建/删除；
- 外部 API 副作用；
- 发布和流量切换；
- 多 Agent 并行产生的独立成果。

源码回滚、数据恢复和外部系统补偿必须分开建模。

### 3.8 多 Agent 只有原则，没有并发控制

“写入 NOTES claim”“用 Git 看冲突”无法提供：

- 原子领取；
- claim 过期；
- owner 身份；
- base revision；
- 写作用域；
- 单一集成者；
- 跨 Agent 回归；
- 失败 Agent 的任务回收。

NOTES 适合广播状态，不适合充当锁。

### 3.9 初始化器不是迁移器，并存在路径安全缺口

`--force` 的行为是：

1. 备份当前目标文件；
2. 用空白模板覆盖；
3. 不合并用户定制内容。

它是“可恢复覆盖”，不是“版本迁移”。

此外，固定相对路径虽然避免了普通 `..` 穿越，但当前实现没有显式拒绝符号链接/reparse point。若目标文件是指向工作区外的链接，`--force` 可能跟随链接覆盖外部文件。

### 3.10 项目没有 dogfood 自己的核心主张

当前仓库没有提供以下证据：

- 一个 fast-track 案例确实没有产生流程资产；
- 一个 Project Mode 案例能跨会话恢复；
- `Plan-ready` 到 `Auto-ready` 的 canary 真能阻止过早移交；
- 再锚定能发现一次人为注入的目标漂移；
- stale gate evidence 会被拒绝；
- 多 Agent claim 不会双领；
- 存量项目升级不会覆盖用户定制。

这是当前最大产品化缺口。

---

## 4. 对 Claude `修复方案-v2.0.md` 的逐项裁决

| 提案 | 裁决 | 原因 |
|---|---|---|
| 声明“行为协议不是硬控制系统” | **接受** | 修辞和能力必须一致 |
| 门控分为硬/证据/判断 | **接受并增强** | 分类正确，但还需 revision、来源和 freshness |
| 所有规则只在 `SKILL.md` 出现一次 | **拒绝原实现** | 外部 skill 不是项目跨会话的可靠运行时依赖 |
| 模板使用一行锚点 | **修改后接受** | 锚点应指向项目内 runtime rules，不应只指外部 skill |
| 用 grep 次数作为去重硬指标 | **拒绝** | 字符串命中不能稳定表示语义重复 |
| checkpoint 增加可复现指针 | **接受并增强** | 命令只是配方，不是通过证据 |
| 无记忆复核者 | **接受为推荐项** | 降低沉没成本，但不等于真正独立验证 |
| pending 人工 gate 阻止 goal 完成 | **改写** | 应阻止“已验收”，不应抹掉“实现完成/自动验证通过” |
| 任一清单项命中即 Project Mode | **拒绝** | 把规模、自主、风险、并发错误合并 |
| AC 数量达到 3 即升级 | **拒绝** | 数量不是复杂度、耦合度或风险的可靠代理 |
| 模式写入 NOTES 首行 | **拒绝** | fast-track 可能没有 NOTES，且违背最小落盘 |
| Phase 后再锚定 | **接受并增强** | 应锚定当前批准 spec，且在交接/压缩/spec delta 后触发 |
| 三次失败/预算过半熔断 | **修改** | 阈值需有预算来源、假设变化和进展定义 |
| 每 Phase 打 tag/branch | **拒绝默认化** | 污染仓库、可能越权，也不覆盖数据与外部副作用 |
| NOTES 中 claim 多 Agent 任务 | **拒绝** | 不原子，容易双领和共享写冲突 |
| 人否决记录 | **接受** | 但应进入统一决策状态机，不必在多模板复制 |
| 旧模板移入 `legacy/` | **拒绝推荐项 A** | 移动后仍是第二真相源；Git 历史已经可追溯 |
| 存量项目用 `--force` 换模板 | **拒绝** | 会覆盖项目定制，不是迁移 |
| 单个小项目完整 dogfood | **不足** | 与 fast-track 定位冲突，无法覆盖长程能力 |

### 4.1 Claude 方案的落地 DAG 自相矛盾

原方案声称 P1、P3、P4 可并行，但：

- P1 修改 `phase3`、`phase4`；
- P2 修改 checkpoint、`phase4`、`phase5`；
- P3 修改 `SKILL.md`、`phase3`、`phase4`；
- P4 修改 `phase1`、`phase3`、`phase4`。

它们在逻辑上部分独立，在写入层面并不独立。若由多个 Agent 直接并行落地，会产生方案自己要求避免的共享写冲突。

---

## 5. v2.0 目标设计原则

### 原则 A：诚实强制

明确区分：

- Skill 可以编排什么；
- harness/CI/权限系统必须强制什么；
- 人必须签字什么。

任何文本规则不得伪装成平台级硬边界。

### 原则 B：一个创作源，多个有目的的投影

不追求“每句话全仓只出现一次”，而追求：

- 一个 canonical authoring source；
- README、Skill 入口、项目运行入口和模板摘要都从它生成或受一致性测试约束；
- 每个投影只保留该读取场景真正需要的内容；
- 关键决策现场允许短摘要重复。

### 原则 C：项目必须能脱离当前会话恢复

已初始化项目不能依赖：

- 当前对话记忆；
- 某个 Agent 曾经加载过 skill；
- 用户机器上恰好安装相同版本的 skill。

跨会话所需的规则和实例值必须存在于项目内。

### 原则 D：模式是多个正交维度，不是一个布尔值

交付规模、自主程度、风险和并发分别判定，最后投影成用户可理解的执行配置。

### 原则 E：证据必须绑定 revision

“通过”必须能回答：

- 什么被验证；
- 验证了哪个代码/配置状态；
- 谁或什么系统验证；
- 在什么环境；
- 原始结果在哪里；
- 当前结果是否仍新鲜。

### 原则 F：状态比“完成/未完成”更诚实

报告必须精确表达当前生命周期状态，禁止用 pending 人工验收抹去已完成的工程事实，也禁止用“实现完成”冒充“已验收”。

### 原则 G：回滚先保护用户成果

任何源码回滚：

- 不默认执行 destructive reset；
- 不覆盖其他 Agent 或用户的独立修改；
- 不把 Git 回滚等同于数据和外部系统恢复。

### 原则 H：流程自身必须接受真实评测

不能只测试模板是否生成，还要测试：

- 路由是否正确；
- 证据是否新鲜；
- 漂移是否被发现；
- 升级是否安全；
- 多 Agent 是否会双领；
- 流程成本是否符合“最轻充分”。

---

## 6. 推荐目标架构

### 6.1 仓库与 Skill 包建议结构

```text
仓库根/
├── agent-sdlc-workflow/             # 唯一进入 .skill 包的产品目录
│   ├── SKILL.md                     # 触发、读取顺序、模式编排；保持短
│   ├── VERSION                      # 单一版本号，例如 2.0.0-rc.1
│   ├── agents/openai.yaml
│   ├── references/
│   │   ├── core-rules.md            # 规则的 canonical authoring source
│   │   ├── mode-model.md            # 正交模式和迁移规则
│   │   ├── evidence-model.md        # gate、证据和完成状态
│   │   ├── recovery-model.md        # 源码/数据/外部系统恢复
│   │   └── multi-agent-model.md     # claim、owner、集成协议
│   ├── assets/templates/
│   │   ├── phase0-constitution.md
│   │   ├── phase1-spec-modeling.md
│   │   ├── phase2-env-gates.md
│   │   ├── phase3-phase-planning.md
│   │   ├── phase4-execution-protocol.md
│   │   ├── phase5-acceptance-retro.md
│   │   ├── TEMPLATE-adr.md
│   │   ├── TEMPLATE-checkpoint.md
│   │   └── TEMPLATE-spec-change.md
│   └── scripts/
│       ├── init_project.py
│       ├── upgrade_project.py
│       └── claim_task.py            # 仅文件型多 Agent 协作时使用
├── scripts/
│   ├── package_skill.py             # 仓库发布工具，不进入 .skill
│   └── verify_package.py
├── tests/
│   ├── test_init_project.py
│   ├── test_upgrade_project.py
│   ├── test_package_skill.py
│   ├── test_content_contracts.py
│   └── test_claim_task.py
├── evals/
│   ├── cases/
│   ├── expected/
│   └── README.md
└── .github/workflows/ci.yml
```

打包脚本使用显式 allowlist，只将 `agent-sdlc-workflow/` 中的运行时文件写入 `.skill`，仓库测试、评测 fixture 和发布工具不进入分发包。

### 6.2 项目初始化后的运行时结构

```text
项目根/
├── AGENTS.md                        # Agent 稳定入口；包含或指向项目内规则快照
├── CONSTITUTION.md                  # 项目采用的纪律和项目特定决定
├── NOTES.md                         # 仅持久项目使用；进度广播，不承担锁职责
├── agent-sdlc.lock.json             # 模板版本、生成基线 hash、迁移依据
├── specs/
├── plans/
│   ├── env-gates-checklist.md
│   ├── execution-protocol.md
│   ├── logs/
│   └── claims/                      # 仅 multi-agent 文件协调模式启用
├── docs/adr/
└── tests/
```

`agent-sdlc.lock.json` 只在 Project Mode 初始化时产生；fast-track 仍不创建任何流程资产。

### 6.3 canonical 与项目快照的关系

推荐机制：

1. `references/core-rules.md` 是 skill 仓库内唯一创作源；
2. `SKILL.md` 指示 Agent 在 Project Mode 必读该文件，但不全文复印；
3. `init_project.py` 将必要规则渲染为项目内 `AGENTS.md` 的受管区块；
4. 受管区块携带 `source_version` 和内容 hash；
5. 模板锚点指向项目内 `AGENTS.md` 对应规则 ID；
6. `upgrade_project.py` 只升级未被用户修改的受管内容；
7. README 可以面向人展开，但由一致性测试验证规则 ID 和关键语义没有漂移。

示例：

```markdown
<!-- agent-sdlc:managed:start source_version=2.0.0 rule_set_hash=... -->
## Agent SDLC 核心规则

- RULE-VAL-001：按局部 → 受影响 → Phase → Goal 逐级扩大验证。
- RULE-USER-001：只有产品语义、外部授权或难恢复操作真正阻塞时询问。
- RULE-SCOPE-001：影响区域用于估算和写冲突协调，不是权限白名单。

完整项目命令和实例值见 `CONSTITUTION.md` 与 `plans/env-gates-checklist.md`。
<!-- agent-sdlc:managed:end -->
```

如果项目已有无受管标记的 `AGENTS.md`：

- 默认不覆盖、不静默追加；
- 生成 `AGENTS.agent-sdlc.md`；
- 输出建议 include/合并位置；
- 只有用户明确同意时才合入原文件。
- 在原 `AGENTS.md` 已合入受管区块或明确引用新文件之前，项目只能是 `Plan-ready`，不得声称 `Auto-ready`。

---

## 7. 模式与状态模型

### 7.1 四个正交维度

#### 维度 1：交付规模

| 值 | 判据 |
|---|---|
| `fast` | 单一可验证增量，预计单会话完成，不需要持久化计划 |
| `project` | 多个可发布增量、跨会话恢复或需要持续计划/DAG |

#### 维度 2：自主程度

| 值 | 判据 |
|---|---|
| `copilot` | 产品语义、风险决策或反馈闭环仍需人参与 |
| `auto` | spec、计划、命令、canary 和恢复入口均已实测 |

#### 维度 3：风险等级

| 值 | 判据 |
|---|---|
| `standard` | 可逆、局部、无生产/真实数据/权限边界影响 |
| `guarded` | 生产、真实数据、迁移、权限、安全、付费或难恢复副作用 |

#### 维度 4：协作方式

| 值 | 判据 |
|---|---|
| `single` | 只有一个写入者 |
| `multi` | 两个及以上 Agent/人会并发写入 |

最终配置可以是：

```text
fast + copilot + guarded + single
project + auto + standard + multi
```

这比把所有情况强制压成 fast/Project 更准确。

### 7.2 路由规则

进入 `project` 的可靠触发：

- 已明确存在两个及以上可独立验收的交付增量；
- 预计需要跨会话恢复；
- 任务 DAG 和持久进度本身产生实际价值；
- 多个里程碑共享同一 spec/架构约束。

以下情况**不单独决定** `project`：

- AC 数量；
- 用户要求 auto；
- 使用多 Agent；
- 单点高风险操作。

它们分别影响自主、并发和风险维度。

### 7.3 动态迁移

| 迁移 | 触发 | 动作 |
|---|---|---|
| `fast → project` | 出现第二个独立增量、需要跨会话、任务依赖显著增长 | 创建最小 spec/plan/NOTES，记录迁移理由 |
| `copilot → auto` | 计划已确认，工程 canary 和所有所需 gate 已实测 | 记录 `Auto-ready` 证据 |
| `auto → copilot` | 熔断、语义冲突、人工判断 gate 阻塞 | 状态改为 `auto_paused`，保留安全可继续任务 |
| `standard → guarded` | 发现生产、数据、权限、安全或难恢复副作用 | 增加恢复与授权 gate |
| `single → multi` | 出现第二个写入者 | 启用 claim 和集成协议 |

fast-track 不要求把模式写进文件。只有发生跨会话或 Project Mode 迁移时才持久化。

---

## 8. 门控、证据与完成状态

### 8.1 三类门控

| 类型 | 含义 | 合法来源 |
|---|---|---|
| `hard` | 外部系统会真正阻断操作 | CI、权限系统、分支保护、部署审批 |
| `evidence` | Agent 可执行，但必须留下与 revision 绑定的原始结果 | 测试、构建、扫描、E2E、运行探针 |
| `judgment` | 需要人的产品、体验、业务或风险判断 | 用户/负责人签字 |

“advisory 建议”不是 gate，不应伪装成阻断条件。

### 8.2 证据记录最小结构

```yaml
gate_id: G-P1-E2E
gate_type: evidence
requirement: AC-3 主路径可完成
revision:
  commit: abc123
  dirty_diff_hash: null
command_or_source: "playwright test checkout"
environment:
  os: windows
  runtime: "node 24.x"
  dependency_lock_hash: "..."
started_at: "2026-07-23T18:20:00Z"
finished_at: "2026-07-23T18:21:12Z"
exit_code: 0
result: passed
artifact:
  pointer: "CI run #123 / local transcript path"
  sha256: "..."
verifier:
  kind: ci
  id: "github-actions"
freshness: exact_revision
```

Markdown checkpoint 可以展示精简列，但底层语义必须覆盖：

| Gate | 类型 | Revision | 命令/来源 | Exit/结论 | 原始证据 | 验证者 | 新鲜度 |
|---|---|---|---|---|---|---|---|

### 8.3 本地无 CI 时的底线

本地命令可以作为复现配方，但要证明“本次曾通过”，还需保存：

- revision 或 dirty diff hash；
- 开始/结束时间；
- exit code；
- 原始输出 transcript；
- 关键产物 hash；
- 工具链/lockfile 标识。

敏感日志不得直接提交仓库；模板应允许：

- 只保存脱敏摘要和 hash；
- 使用忽略目录；
- 记录外部安全存储指针；
- 设置保留期限。

### 8.4 证据复用规则

证据只有在以下条件下可复用：

1. 当前 revision 与证据 revision 相同；或
2. 中间变化已被影响分析证明不涉及该 gate 的代码、配置、测试输入和环境；且
3. 证据产物仍可访问；且
4. gate 定义没有变化。

不能仅凭“我认为没关系”复用；checkpoint 至少记录复用理由。

### 8.5 生命周期状态

推荐状态：

```text
planned
→ implementation_complete
→ automated_verified
→ human_acceptance_pending
→ accepted
→ released
→ closed
```

允许的最终报告示例：

> 实现和自动门控已完成；AC-5 的人工体验验收仍待用户签字，因此当前状态为 `human_acceptance_pending`，不是 `accepted`。

禁止：

- 有 pending 判断 gate 时报告“已验收”；
- 因人工 gate pending 而隐瞒实现和自动验证已完成；
- 用 `deferred` 偷偷绕过原本承诺的 AC，除非 spec delta 已批准。

---

## 9. 再锚定与熔断

### 9.1 锚点优先级

再锚定不应只对照“用户最初一句话”，应按以下顺序：

1. 当前已批准 spec；
2. 已批准 spec delta；
3. 当前 goal 和非目标；
4. 用户原始意图，用于检查合法变更是否仍服务于最初价值。

否则，已经批准的需求演化会被错误识别成漂移。

### 9.2 触发时机

以下节点必须再锚定：

- Phase 退出后；
- 上下文压缩或新会话恢复后；
- Agent 交接后；
- spec delta 合并后；
- 影响面显著扩大后；
- 最终验收前。

长 Phase 可在 checkpoint 触发，不必等到 Phase 结束。

### 9.3 再锚定检查

```markdown
## 再锚定

1. 当前每项工作能否追溯到有效 AC、风险缓解或已批准 delta？
2. 当前 diff 是否触碰任何非目标？
3. 是否新增了未在 spec/ADR 中出现的外部接口、数据状态或权限语义？
4. 当前测试是否证明用户价值，还是只证明内部实现自洽？
5. 剩余任务是否仍是完成 goal 的必要条件？
6. 若用户只看当前可观察行为，是否会认为它解决了批准的问题？
```

处理顺序：

```text
发现疑似漂移
→ Agent 先做影响分析和可逆修正
→ 能由现有 spec 唯一决定则自行处理
→ 必须改变产品语义时才提交 spec delta / 询问用户
```

再锚定不应变成新的高频打扰源。

### 9.4 熔断模型

熔断状态为 `auto_paused`，不是“项目失败”。

触发条件：

1. 同一失败再次发生，且本轮没有新假设、新证据或新实验；
2. 已验证多个独立假设仍无进展，达到项目配置的探索上限；
3. 已配置预算且 burn rate 与通过 gate 的增量显著失衡；
4. 需要撤销多个已验收任务才能继续；
5. 再锚定连续发现同一类目标偏差；
6. 当前操作进入未获授权的 guarded 范围。

若平台不能提供 token/time 预算，就不得假装能执行“预算过半”规则。预算 gate 只有在起始值和观测方式明确时启用。

熔断报告：

```markdown
- 当前状态：auto_paused
- 已完成且仍有效的增量：
- 失效/可疑的增量：
- 已验证假设：
- 新获得的证据：
- 未解决决策：
- 推荐：继续探索 / 调整 spec / 回退 / 人工接管
```

---

## 10. 安全回退与恢复

### 10.1 三类恢复必须分离

| 类别 | 对象 | 机制 |
|---|---|---|
| 源码恢复 | 文件、commit、配置 | revert、显式文件恢复、受控分支 |
| 数据恢复 | schema、数据写入、migration | down migration、备份恢复、补偿脚本 |
| 外部副作用恢复 | 云资源、发布、API 调用、消息 | provider rollback、补偿事务、人工处置 |

不能用“回到 Git 绿色点”概括后三者。

### 10.2 版本控制策略先实例化

Project Mode 初始化时选择：

| 策略 | 含义 |
|---|---|
| `record-only` | Agent 只记录当前 commit/diff，不自行 commit/tag |
| `agent-commit` | 用户已授权 Agent 按任务/Phase 提交 |
| `branch-per-agent` | 多 Agent 各自在 branch/worktree 工作，由集成者合并 |

没有授权时，Skill 不应默认创建 tag、branch 或 commit。

### 10.3 已知良好点

已知良好点至少记录：

- commit 或受控 diff 标识；
- dirty worktree 状态；
- 对应 gate evidence；
- 尚未覆盖的数据/外部副作用；
- 回退所需授权；
- 其他 Agent 的并行成果。

### 10.4 源码回退规则

默认优先级：

1. revert 当前 Agent 自己拥有的独立 commit；
2. 恢复明确列出的文件/区块；
3. 在隔离 branch/worktree 中重新构建；
4. 只有用户明确授权且确认无他人成果时，才考虑整体回退。

禁止默认：

- `git reset --hard`；
- 覆盖未提交用户修改；
- 回退其他 Agent 的已验收成果；
- 把源码恢复报告成数据/生产恢复完成。

---

## 11. 多 Agent 协作协议

### 11.1 原则

- NOTES 是广播板，不是锁；
- task claim 必须原子创建；
- 每个任务有唯一 owner；
- 每个共享写点有唯一串行 owner；
- 每个 Agent 产出可独立集成的交付包；
- 合并后必须由集成者跑跨 Agent 回归。

### 11.2 文件型 claim

若没有 issue tracker/调度器，使用：

```text
plans/claims/<task-id>.json
```

最小字段：

```json
{
  "task_id": "T3",
  "owner": "agent-17",
  "base_revision": "abc123",
  "write_scope": ["src/auth/", "tests/auth/"],
  "shared_write_lane": null,
  "claimed_at": "2026-07-23T18:00:00Z",
  "lease_expires_at": "2026-07-23T20:00:00Z",
  "status": "active"
}
```

`claim_task.py` 使用 exclusive create；文件已存在时领取失败，而不是最后写入者覆盖前一个 claim。

### 11.3 租约与回收

- claim 必须带失效时间；
- Agent checkpoint 时续租；
- 失效 claim 不能由普通 Agent直接覆盖；
- 集成者或协调者记录回收原因后释放；
- 恢复任务必须读取原 owner 的 checkpoint 和 diff。

### 11.4 写冲突和共享写点

以下默认串行：

- migration；
- 公共 schema；
- lockfile；
- 全局配置；
- 发布清单；
- 相同核心模块的大范围重构。

“影响区域”仍不是权限白名单，但 Agent 扩大写入范围时必须：

1. 检查是否与活跃 claim 冲突；
2. 无冲突则更新 claim；
3. 有冲突则协调或交给集成者。

### 11.5 交付包

每个 Agent 交付：

- task/AC；
- base revision；
- 最终 revision 或 diff；
- 修改摘要；
- gate evidence；
- 新 ADR/spec delta；
- 未解决风险；
- 推荐集成顺序。

集成者负责：

- 解决语义冲突；
- 运行跨任务/跨 Agent 回归；
- 确认 claim 已释放；
- 产出 Phase checkpoint。

---

## 12. 初始化、升级与打包修复

### 12.1 修复 Skill 内脚本调用路径

当前 `SKILL.md` 中的：

```text
python scripts/init_project.py <项目根目录>
```

依赖当前工作目录恰好是 skill 目录。应改为：

- Agent 根据已加载 `SKILL.md` 的实际位置解析脚本绝对路径；或
- 文档使用 `<skill-root>/scripts/init_project.py` 占位语义；
- README 的仓库内命令与安装后调用分别说明。

### 12.2 `init_project.py` 安全要求

新增前置检查：

1. 所有目标规范化后仍位于项目根；
2. 目标文件和父目录不是 symlink/reparse point，或解析后仍在根内；
3. 文件目标不能是目录；
4. 写入采用临时文件 + 原子替换；
5. dry-run 输出创建、跳过、冲突和风险；
6. 失败时不留下部分生成状态；
7. 已有无标记 `AGENTS.md` 默认不覆盖。

`--force` 保留为明确的“备份后覆盖”工具，但：

- 文档必须标注它不是升级；
- 不得在迁移指南中推荐；
- 遇到 symlink/reparse point 必须拒绝；
- 应要求额外确认参数，例如 `--force --acknowledge-overwrite`，避免误触。

### 12.3 新增 `upgrade_project.py`

升级依据：

```json
{
  "skill_version": "1.5.0",
  "generated_files": {
    "plans/execution-protocol.md": {
      "template": "phase4-execution-protocol.md",
      "baseline_sha256": "..."
    }
  }
}
```

升级算法：

1. 读取 lock 和目标版本；
2. 比较当前文件与旧 baseline；
3. 未修改文件可直接升级；
4. 用户已修改文件不覆盖，生成 `.agent-sdlc-upgrade/<path>.new` 和差异报告；
5. 受管区块可按 marker 单独升级；
6. 所有拟写入内容先 dry-run；
7. 应用前备份；
8. 任一步失败则保持原版本和 lock 不变；
9. 升级完成后运行内容契约测试。

### 12.4 确定性打包

新增 `package_skill.py`：

- 文件列表显式；
- 路径排序固定；
- 压缩参数固定；
- archive timestamp 固定或规范化；
- 不打入缓存、IDE、测试输出和旧模板；
- 构建两次应得到相同 SHA-256。

新增 `verify_package.py`：

- 包内容与源码逐文件 hash 一致；
- 必需文件齐全；
- 无额外文件；
- YAML/frontmatter 可解析；
- `VERSION`、README 和包元数据一致。

### 12.5 旧模板包处理

推荐选择原方案的 C：

- 从主分支删除 `agent开发工作流模板包/`；
- Git 历史负责溯源；
- README/changelog 用一段文字记录它已被合并；
- 若确需发布旧版，使用 Git tag 或 release archive，而不是把第二套规则长期放在仓库树中。

---

## 13. 测试与评测方案

### 13.1 单元测试

#### 初始化器

- dry-run 不写盘；
- 首次创建内容正确；
- 重复执行幂等；
- force 备份可恢复；
- 已有 `AGENTS.md` 不覆盖；
- symlink 文件目标被拒绝；
- symlink 父目录被拒绝；
- 目录占据文件目标时给出明确错误；
- 非目录项目根被拒绝；
- 中途失败不留下部分状态；
- Unicode 路径和内容正确。

#### 升级器

- 未修改模板自动升级；
- 已修改模板保留并生成冲突报告；
- 受管区块可升级；
- 无 marker 文件不被静默改写；
- lock 只在全部成功后更新；
- dry-run 零写入；
- 备份完整；
- v1.5 → v2.0 fixture 可重复升级。

#### 打包

- 两次构建 hash 相同；
- 包与源码一致；
- 缺文件或多文件时失败；
- 版本不一致时失败。

#### 多 Agent claim

- 同一任务只能有一个成功 claim；
- claim 续租；
- 过期 claim 需协调者回收；
- write scope 扩展会检测冲突；
- release 后可重新领取。

### 13.2 内容契约测试

不要测试“某句话只出现一次”，而应测试：

- 每条核心规则有稳定 rule ID；
- 模板引用的 rule ID 均存在；
- 项目 runtime snapshot 可由 canonical source 生成；
- README 的关键术语与 canonical 版本一致；
- 所有模板引用路径有效；
- checkpoint 包含 revision、来源、结果、证据和 verifier；
- 不存在指向外部安装路径的项目内必要锚点；
- fast-track 文本不要求创建 NOTES/spec/checkpoint；
- `--force` 未被描述为升级方式。

### 13.3 场景评测矩阵

| Case | 场景 | 预期 |
|---|---|---|
| E1 | 单文件小修复 | 留在 fast；不生成流程文件；运行最小充分验证 |
| E2 | 单点生产配置修复 | `fast + guarded`；不因高风险强制全 Project，但需授权/恢复 gate |
| E3 | 多里程碑单 Agent 项目 | `project + copilot`，canary 后才可 auto |
| E4 | 跨会话恢复 | 新会话只读项目文件即可恢复 goal、规则、证据和下一步 |
| E5 | 注入 spec 变化 | 触发影响分析和 spec delta，不静默改变 AC |
| E6 | 注入目标漂移 | 再锚定在最终验收前发现 |
| E7 | stale 绿色证据 | 代码改变后旧 gate 不得被当作 exact evidence |
| E8 | 人工体验待签字 | 正确报告 `human_acceptance_pending` |
| E9 | 重复失败 | 无新信息时暂停 auto，不机械跑满三次 |
| E10 | 两 Agent 双领 | 只有一个 claim 成功 |
| E11 | 共享 schema 写入 | 强制进入串行 lane |
| E12 | 存量项目升级 | 用户定制零丢失，冲突显式报告 |

### 13.4 评测指标

| 指标 | 含义 |
|---|---|
| 路由正确率 | 是否选择了最轻充分配置 |
| 无效流程资产数 | fast-track 是否产生不必要文件 |
| 用户打断次数 | 可由 Agent/证据解决的问题是否打扰用户 |
| stale evidence 拒绝率 | 旧证据是否被正确判定失效 |
| 漂移发现时点 | 漂移在第几个 checkpoint 被发现 |
| 恢复时间 | 新会话到正确继续执行所需步骤 |
| 双领率 | 多 Agent 同一任务被重复领取的比例 |
| 升级保真率 | 存量定制内容保留比例，目标 100% |
| 包可重复性 | 相同源码是否生成相同包 hash |
| token/时间成本 | 相比 v1.5 的流程成本变化 |

不能只跑一个“小项目完整流程”。至少要同时覆盖 E1 和 E3/E4，并对 E5–E10 做故障注入。

---

## 14. 详细实施计划

### Phase 0：冻结设计契约

**目标**：先决定语义，避免边改模板边重新发明概念。

任务：

1. 定义四维模式模型；
2. 定义 gate/evidence schema；
3. 定义生命周期状态；
4. 定义源码/数据/外部恢复边界；
5. 定义 canonical → 项目 runtime snapshot 的生成关系；
6. 为上述定义分配稳定 rule ID；
7. 评审并确认 `--force` 不承担迁移职责。

退出门控：

- [ ] 每个术语只有一个明确含义；
- [ ] 没有项目运行时必须依赖外部 skill 的规则；
- [ ] fast-track 不要求落盘；
- [ ] `implementation_complete` 与 `accepted` 明确分离。

### Phase 1：先补工具链与安全测试

**目标**：在大改模板前，让仓库具备自证能力。

任务：

1. 增加 CI；
2. 增加 `package_skill.py` 和 `verify_package.py`；
3. 为现有包建立确定性构建测试；
4. 为初始化器增加 symlink、目录冲突、原子写测试；
5. 新增 `VERSION`；
6. 增加内容引用和版本一致性测试。

退出门控：

- [ ] 测试和打包由 CI 强制；
- [ ] 相同源码两次打包 hash 相同；
- [ ] 包内容和源码自动比对；
- [ ] 初始化器不会通过链接写出项目根。

### Phase 2：重构 canonical 与模板

**目标**：解决冗余，但不破坏项目内恢复。

任务：

1. 新增 `references/core-rules.md`；
2. 将 `SKILL.md` 收敛为触发、路由、读取顺序和阶段编排；
3. 初始化器生成项目内 runtime rules snapshot；
4. phase 模板改用 rule ID + 本地入口摘要；
5. 删除 Python 内的大段手写规则副本，改由受管模板渲染；
6. README 与 canonical 建立一致性测试；
7. 删除旧模板包。

退出门控：

- [ ] 项目新会话无需外部 skill 也能恢复核心运行规则；
- [ ] 规则创作源唯一；
- [ ] 各读取现场仍保留最小充分摘要；
- [ ] 仓库中不再存在第二套可执行模板。

### Phase 3：模式、证据和状态落地

**目标**：把最核心的运行语义从散文升级为结构。

任务：

1. 实现四维模式判定模板；
2. fast-track → project 迁移协议；
3. 更新 checkpoint evidence schema；
4. 更新 Phase/Goal gate；
5. 引入生命周期状态；
6. 更新 `Plan-ready` / `Auto-ready` 证据要求；
7. 更新人工 gate 的 pending/approved/rejected 记录。

退出门控：

- [ ] E1、E2、E3、E7、E8 通过；
- [ ] 命令本身不会被误记为 passed evidence；
- [ ] 人工 gate pending 时报告状态准确；
- [ ] AC 数量不参与硬编码路由。

### Phase 4：再锚定、熔断、恢复和多 Agent

**目标**：补齐长程自治的真实失效模式。

任务：

1. 实现基于当前批准 spec 的再锚定；
2. 增加交接、压缩和 delta 后触发；
3. 增加 `auto_paused` 熔断报告；
4. 定义三类恢复；
5. 增加 VCS 策略实例化；
6. 实现原子 task claim；
7. 定义集成者和共享写 lane。

退出门控：

- [ ] E4、E5、E6、E9、E10、E11 通过；
- [ ] 不默认创建 tag/branch/commit；
- [ ] 不把 Git 恢复冒充数据/生产恢复；
- [ ] NOTES 不承担 claim 锁职责。

### Phase 5：安全升级与 RC dogfood

**目标**：证明 v2.0 能升级真实存量项目，并在真实流程中有效。

任务：

1. 实现 `upgrade_project.py`；
2. 建立 v1.5 fixture；
3. 跑 E12；
4. 发布 `2.0.0-rc.1`，不直接宣称稳定版；
5. 选择一个真实 fast-track 和一个真实跨会话项目 dogfood；
6. 记录路由、token、用户打断、漂移、证据复用和恢复数据；
7. 根据数据调整模板；
8. 通过 release gate 后发布 `2.0.0`。

退出门控：

- [ ] 存量项目定制内容零丢失；
- [ ] 两类真实项目均有可复查数据；
- [ ] 所有 CI、内容契约和 eval 通过；
- [ ] `.skill` 包由确定性脚本生成；
- [ ] changelog 明确破坏性语义变化。

### 14.1 并行实施约束

允许并行的是研究和独立文件实现，不允许多个 Agent 同时直接修改核心模板。

推荐写入分区：

| Owner | 写入范围 |
|---|---|
| A：工具链 | `scripts/package_*`、CI、package tests |
| B：升级安全 | `agent-sdlc-workflow/scripts/init_project.py`、`upgrade_project.py` 及对应测试 |
| C：规则模型 | `agent-sdlc-workflow/references/`，不直接改 phase 模板 |
| Integrator | `agent-sdlc-workflow/SKILL.md`、phase0–5、README、最终包 |

规则模型稳定后，由单一 Integrator 把内容投影到 `SKILL.md` 和阶段模板，避免 P1/P3/P4 式共享写冲突。

---

## 15. v2.0 验收标准

| AC | 验收标准 |
|---|---|
| AC-01 | 已初始化项目在没有当前会话记忆、没有重新加载 skill 的条件下，仍可恢复核心规则、目标、状态和下一步 |
| AC-02 | fast-track 小任务不会强制创建 NOTES/spec/plan/checkpoint |
| AC-03 | 规模、自主、风险和并发分别判定；AC 数量不作为硬路由条件 |
| AC-04 | 每个 evidence gate 绑定 revision、来源、结果、原始证据和 verifier |
| AC-05 | 系统区分实现完成、自动验证、人工待验收、已验收和已发布 |
| AC-06 | 再锚定以当前批准 spec/delta 为主，不把合法需求演化误判为漂移 |
| AC-07 | 熔断进入 `auto_paused`，保留已验证增量并给出恢复入口 |
| AC-08 | 源码、数据和外部副作用恢复分别建模；不默认 destructive reset |
| AC-09 | 同一任务的并发 claim 只有一个成功，过期 claim 可审计回收 |
| AC-10 | `--force` 不再被推荐为升级方式，目标 symlink/reparse point 被拒绝 |
| AC-11 | v1.5 存量项目升级时，用户定制内容 100% 保留或显式进入冲突报告 |
| AC-12 | 相同源码两次生成的 `.skill` 包 hash 完全一致 |
| AC-13 | 旧模板包不再存在于主仓库树，历史通过 Git tag/commit 追溯 |
| AC-14 | CI 覆盖初始化、升级、打包、内容契约和多 Agent claim |
| AC-15 | 至少一个 fast-track 和一个跨会话 Project Mode 真实 dogfood 有量化结果 |

---

## 16. 风险与权衡

### 16.1 项目内规则快照会增加一个持久资产

这是有意成本。跨会话恢复不能依赖外部 skill 恰好存在。通过受管区块和 lock hash 控制腐烂风险。

### 16.2 证据字段增加记录成本

只对 Project Mode 的 Phase/Goal gate 强制完整 envelope；fast-track 最终摘要可以精简，但仍需说明 revision、命令和结果。

### 16.3 四维模式比二元模式复杂

内部模型更准确，用户界面仍可总结成：

> 本任务走 fast-track；由于涉及生产配置，启用 guarded gates；保持 copilot。

复杂度应留在系统内部，不把表格甩给用户填写。

### 16.4 文件型 claim 增加工具代码

只有 multi-agent 项目启用。若已有 issue tracker 或调度器提供原子 claim，应复用现有系统，不再创建文件 claim。

### 16.5 确定性打包会限制 archive 元数据

这是可接受权衡：可复现发布和供应链审计比保留本地文件时间戳更重要。

### 16.6 独立复核仍可能共享模型盲点

新上下文只降低沉没成本和记忆偏差；关键安全/业务 gate 仍应由不同工具、CI、领域负责人或真实环境验证。

---

## 17. 最终建议

### 17.1 对当前项目

项目值得继续投入。它不是普通模板堆砌，已经形成两项有辨识度的核心资产：

- 验证质量决定自治上限；
- 计划完成不等于自治就绪。

但 v2.0 不应继续只优化措辞和表格。真正的版本跃迁应来自：

- 运行时真相源；
- revision-bound evidence；
- 生命周期状态；
- 安全迁移；
- 确定性发布；
- 多场景实证。

### 17.2 对 Claude 方案

将其作为**高质量问题清单**保留，不应直接作为实施 spec。

建议处理方式：

```text
接受诊断
→ 替换 canonical 架构
→ 替换二元路由
→ 增强证据和状态模型
→ 重写回滚/迁移/多 Agent
→ 按本文 Phase 0–5 实施
```

### 17.3 发布判断

在以下条件完成前，不建议直接把 README 从 v1.5 改成稳定版 v2.0：

- 安全升级器可用；
- 包可重复构建；
- CI 生效；
- E1–E12 场景至少在 fixture 中通过；
- 两个真实 dogfood 有结果；
- 存量项目迁移无定制丢失。

先发布 `2.0.0-rc.1`，让 v2.0 用自己的门控证明自己，而不是再次用一份方案文档自证。

---

## 附录 A：建议删除或废弃的内容

1. `agent开发工作流模板包/`
2. Python `AGENTS_MD` 中手写复制的完整规则段
3. “AC ≥ 3 自动升级”规则
4. fast-track 写 NOTES 首行规则
5. “命令本身可充当通过证据”的表述
6. 默认每 Phase 创建 Git tag/branch
7. NOTES claim
8. 把 `--force` 描述为存量升级路径
9. 用 `grep -rc` 次数充当语义一致性的核心验收

## 附录 B：建议保留并升级的术语

| 现有术语 | 处理 |
|---|---|
| Fast-track | 保留，作为交付规模的用户界面 |
| Project Mode | 保留，内部拆为四维配置 |
| Plan-ready | 保留，明确所需 spec/plan 证据 |
| Auto-ready | 保留，必须绑定 canary 和 gate evidence |
| Phase | 保留，只用于可验证交付增量 |
| Goal | 保留，增加生命周期状态 |
| Checkpoint | 保留，升级为恢复入口和证据索引 |
| Spec delta | 保留，增加批准状态和再锚定关系 |
| 退出门控 | 保留，但每项显式标注 hard/evidence/judgment |
